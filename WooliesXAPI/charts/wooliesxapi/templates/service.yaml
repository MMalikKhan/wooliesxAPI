apiVersion: v1
kind: Service
metadata:
  name: {{ template "wooliesxapi.fullname" . }}
  labels:
    app: {{ template "wooliesxapi.name" . }}
    chart: {{ template "wooliesxapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "wooliesxapi.name" . }}
    release: {{ .Release.Name }}
